# IQ Group — Placeholder Site

Minimal static "we exist" placeholder for infoquestbd.com. Plain HTML/CSS, no build step.

Deploy:

```
cd site && vercel --prod
```

After deploying, point infoquestbd.com's DNS at the Vercel project.
